<Project Sdk="Microsoft.NET.Sdk">

  <!-- Test project for the committed stubs. It is never packaged and never referenced by the
       library: Ondewo.VTSI.Client.csproj removes tests/** from its Compile glob. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <IsPackable>false</IsPackable>
    <AssemblyName>Ondewo.VTSI.Client.Tests</AssemblyName>
    <!-- Nested under the GENERATED namespace spelling (Ondewo.Vtsi), not under the package id
         (Ondewo.VTSI): two namespaces differing only in case are a trap for case-insensitive
         consumers. -->
    <RootNamespace>Ondewo.Vtsi.Client.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
    <!-- Drives `make test`'s coverage gate. PrivateAssets=all so it can never leak into a
         package that referenced this project. -->
    <PackageReference Include="coverlet.msbuild" Version="6.0.2" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../Ondewo.VTSI.Client.csproj" />
  </ItemGroup>

</Project>
